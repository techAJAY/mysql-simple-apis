const express = require('express')
const router  = express.Router()
const userController  = require("../controllers/userController")

// router.get("/getdata",userController.sel)

module.exports = router